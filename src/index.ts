/** A wrapper around the Blitztime API, in Typescript. */
export { HttpClient } from './endpoints';
export { EventName, TimerConnection } from './socket';
export {
    ApiError,
    AppStats,
    SocketCredentials,
    StageSettings,
    Timer,
    TimerJoinSide,
    TimerSide
} from './types';
